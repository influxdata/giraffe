# Installation

Giraffe is published to npm.
To use Giraffe, add it as a dependency to your React app:

```
yarn add @influxdata/giraffe
```

Or, if you're using npm:

```
npm install @influxdata/giraffe
```
